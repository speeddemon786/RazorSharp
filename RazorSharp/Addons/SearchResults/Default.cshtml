@{
    var message = string.Empty;

    IEnumerable<dynamic> data;
    using (var db = Database.Open(Functions.GetDbName()))
    {
        const string sqlSelect = "Select * From Widgets Where wName = 'search-results'";
        data = db.Query(sqlSelect);
    }
    if (!data.Any())
    {
        Response.Redirect("~/Admin/SearchResults/Install");
    }
}
@section Scripts {

}
@section Menu{
    @RenderPage("~/Admin/_Menu.cshtml")
}
@Html.Raw(message)
<form method="post" action="">
    <div class="row">
        <div class="col-lg-12">
            <h2 class="page-header">Search Results Settings</h2>
            <p>Under construction...</p>
        </div>
    </div>
</form>